# key: <s
# name: begin_src
# --
#+begin_src ${1:$$(yas-choose-value (let (lang) (dolist (item org-babel-load-languages) (when (cdr item) (push (symbol-name (car item)) lang))) lang))} -n
$0
#+end_src